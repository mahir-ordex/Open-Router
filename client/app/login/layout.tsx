export const metadata = {
  title: "Sign in — Aperture",
  description: "Sign in to Aperture to route requests across models and providers.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
