export const metadata = {
  title: "Create account — Aperture",
  description: "Create an Aperture account to get an API key and start routing models.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
